apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: semantic-mapper-state
  labels:
    app.kubernetes.io/name: semantic-mapper
    app.kubernetes.io/component: api
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
