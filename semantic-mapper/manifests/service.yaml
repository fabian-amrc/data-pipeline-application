apiVersion: v1
kind: Service
metadata:
  name: semantic-mapper
  labels:
    app.kubernetes.io/name: semantic-mapper
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: semantic-mapper
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
